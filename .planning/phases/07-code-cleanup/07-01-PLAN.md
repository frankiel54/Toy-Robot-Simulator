---
phase: 07-code-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ToyRobot/ParsedCommand.cs
  - ToyRobot/CommandParser.cs
  - ToyRobot/Table.cs
  - ToyRobot/Simulator.cs
  - ToyRobot/Application.cs
  - ToyRobot.Tests/CommandParserTests.cs
autonomous: false
requirements: [REQ-07-01]
must_haves:
  truths:
    - "ParsedCommand.cs has no unused using directives"
    - "ParsedCommand and CommandOptions are immutable positional record types"
    - "TryParsePlaceArgs is private in CommandParser"
    - "Table._width and _height are declared readonly"
    - "Simulator exposes IsPlaced as a read-only property (not a method)"
    - "Simulator.Report() throws InvalidOperationException when the robot has not been placed"
    - "No Application.cs WriteLine call has a leading escaped-newline in its string argument"
  artifacts:
    - path: "ToyRobot/ParsedCommand.cs"
      provides: "Record types CommandOptions and ParsedCommand with no unused using directives"
      contains: "public record ParsedCommand"
    - path: "ToyRobot/CommandParser.cs"
      provides: "Private TryParsePlaceArgs; positional CommandOptions constructor"
      contains: "private static bool TryParsePlaceArgs"
    - path: "ToyRobot/Table.cs"
      provides: "Readonly width and height fields"
      contains: "readonly int _width"
    - path: "ToyRobot/Simulator.cs"
      provides: "IsPlaced property; Report() guard"
      contains: "public bool IsPlaced"
    - path: "ToyRobot/Application.cs"
      provides: "Clean WriteLine calls; IsPlaced property access"
    - path: "ToyRobot.Tests/CommandParserTests.cs"
      provides: "No direct test of the now-private TryParsePlaceArgs"
  key_links:
    - from: "ToyRobot/CommandParser.cs"
      to: "ToyRobot/ParsedCommand.cs"
      via: "ParseCommand uses positional record constructor new CommandOptions(x, y, facing)"
      pattern: "new CommandOptions\\(x, y, facing\\)"
    - from: "ToyRobot/Application.cs"
      to: "ToyRobot/Simulator.cs"
      via: "Call site uses _simulator.IsPlaced (property, not method call)"
      pattern: "_simulator\\.IsPlaced"
---

<objective>
Fix all 9 code smells identified in the post-phase-6 review. Changes are left uncommitted so the developer can review the diff and commit before Wave 2 begins.

Purpose: Eliminate structural code quality issues — unused imports, mutable value types, exposed internals, missing readonly modifiers, and leading escaped-newline strings.
Output: Modified ParsedCommand.cs, CommandParser.cs, Table.cs, Simulator.cs, Application.cs, and CommandParserTests.cs with all 9 smells resolved. Changes are NOT committed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-code-cleanup/07-CONTEXT.md
@CLAUDE.md
@ToyRobot/ParsedCommand.cs
@ToyRobot/CommandParser.cs
@ToyRobot/Table.cs
@ToyRobot/Simulator.cs
@ToyRobot/Application.cs
@ToyRobot.Tests/CommandParserTests.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove unused using directives (Smell 1)</name>
  <files>ToyRobot/ParsedCommand.cs, ToyRobot.Tests/CommandParserTests.cs</files>
  <action>
    In ToyRobot/ParsedCommand.cs, remove the three unused top-level using directives: System, System.Collections.Generic, and System.Text. After removal, the file begins directly with the namespace declaration. No other content changes.

    In ToyRobot.Tests/CommandParserTests.cs, remove the four unused top-level using directives: System, System.Collections.Generic, System.ComponentModel.Design, and System.Text. Retain only the Xunit using directive. No other content changes.
  </action>
  <verify>dotnet run --project ToyRobot.Tests</verify>
  <acceptance_criteria>
    - grep -c "using System;" ToyRobot/ParsedCommand.cs returns 0
    - grep -c "using System.Collections" ToyRobot/ParsedCommand.cs returns 0
    - grep -c "using System.Text" ToyRobot/ParsedCommand.cs returns 0
    - grep -c "using System.ComponentModel.Design" ToyRobot.Tests/CommandParserTests.cs returns 0
    - dotnet run --project ToyRobot.Tests exits with code 0
  </acceptance_criteria>
  <done>All unused using directives removed from both files; all existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Convert CommandOptions and ParsedCommand to records; make TryParsePlaceArgs private (Smells 2, 3, 4, 6)</name>
  <files>ToyRobot/ParsedCommand.cs, ToyRobot/CommandParser.cs, ToyRobot.Tests/CommandParserTests.cs</files>
  <action>
    In ToyRobot/ParsedCommand.cs:
    - Convert the CommandOptions class to a positional record: public record CommandOptions(int X, int Y, Direction Facing);
      This replaces the class with mutable { get; set; } properties. The positional record gives init-only semantics automatically.
    - Convert the ParsedCommand class to a positional record with a default parameter: public record ParsedCommand(CommandType Type, CommandOptions? Options = null);
      Remove the hand-written constructor entirely — the record primary constructor replaces it.

    In ToyRobot/CommandParser.cs:
    - Change the TryParsePlaceArgs access modifier from public to private (per Smell 3 in CONTEXT.md).
    - In ParseCommand, update the CommandOptions construction from the object-initialiser form to the positional record constructor: new CommandOptions(x, y, facing). The existing variable names x, y, and facing match the record parameter names exactly.

    In ToyRobot.Tests/CommandParserTests.cs:
    - Remove the test method that directly invokes TryParsePlaceArgs (the Theory marked with MemberData referencing TryParsePlaceArgsData) along with its static TheoryData provider method. This coverage is already supplied by ParseCommand_Should_Parse_Place_Options. Do not remove any other test methods.
  </action>
  <verify>dotnet run --project ToyRobot.Tests</verify>
  <acceptance_criteria>
    - grep -c "public record ParsedCommand" ToyRobot/ParsedCommand.cs returns 1
    - grep -c "public record CommandOptions" ToyRobot/ParsedCommand.cs returns 1
    - grep -c "private static bool TryParsePlaceArgs" ToyRobot/CommandParser.cs returns 1
    - grep -c "new CommandOptions(" ToyRobot/CommandParser.cs returns 1
    - grep -c "TryParsePlaceArgsData" ToyRobot.Tests/CommandParserTests.cs returns 0
    - dotnet run --project ToyRobot.Tests exits with code 0
  </acceptance_criteria>
  <done>Both types are immutable positional records; TryParsePlaceArgs is private; its direct test and theory data are removed; all remaining tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Add readonly to Table fields; replace IsRobotPlaced() with IsPlaced property; add Report() guard (Smells 5, 7, 8)</name>
  <files>ToyRobot/Table.cs, ToyRobot/Simulator.cs, ToyRobot/Application.cs</files>
  <action>
    In ToyRobot/Table.cs:
    - Add the readonly modifier to both private fields: private readonly int _width and private readonly int _height. No other changes.

    In ToyRobot/Simulator.cs:
    - Replace the IsRobotPlaced() method with a read-only expression-body property: public bool IsPlaced => RobotPlaced;
      Keep the existing private bool RobotPlaced { get; set; } backing property unchanged.
    - At the top of the Report() method body, add a guard: if (!RobotPlaced) throw new InvalidOperationException("Robot has not been placed.");
      Place this guard as the first statement before the return/interpolation.

    In ToyRobot/Application.cs:
    - Find the single call site of the now-removed IsRobotPlaced() method (the condition in the if-check before the switch statement) and change it to use the property: replace _simulator.IsRobotPlaced() with _simulator.IsPlaced (drop the parentheses). No other changes to this file in this task.
  </action>
  <verify>dotnet run --project ToyRobot.Tests</verify>
  <acceptance_criteria>
    - grep -c "readonly int _width" ToyRobot/Table.cs returns 1
    - grep -c "readonly int _height" ToyRobot/Table.cs returns 1
    - grep -c "public bool IsPlaced" ToyRobot/Simulator.cs returns 1
    - grep -c "IsRobotPlaced" ToyRobot/Simulator.cs returns 0
    - grep -c "IsRobotPlaced" ToyRobot/Application.cs returns 0
    - grep -c "InvalidOperationException" ToyRobot/Simulator.cs returns 1
    - dotnet run --project ToyRobot.Tests exits with code 0
  </acceptance_criteria>
  <done>Table fields are readonly; Simulator exposes IsPlaced property; Report() throws on unplaced robot; Application.cs call site updated; all tests pass.</done>
</task>

<task type="auto">
  <name>Task 4: Fix leading escaped-newline strings in Application.cs WriteLine calls (Smell 9)</name>
  <files>ToyRobot/Application.cs</files>
  <action>
    In ToyRobot/Application.cs, locate the two WriteLine calls whose string arguments begin with a backslash-n escape sequence:
    1. Inside the else branch of the Place handling block (the invalid-PLACE-arguments message).
    2. Inside the default case of the switch statement (the invalid-selection message).

    For each location, replace the single _output.WriteLine("...") call that starts with a backslash-n sequence with two consecutive calls: first an argumentless _output.WriteLine() to emit the blank line, then a second _output.WriteLine("...") containing only the message text without any leading escape sequence.

    Do not change any other content in this file.
  </action>
  <verify>dotnet run --project ToyRobot.Tests</verify>
  <acceptance_criteria>
    - grep -rn 'WriteLine("\\n' ToyRobot/Application.cs returns no matches
    - dotnet run --project ToyRobot.Tests exits with code 0
  </acceptance_criteria>
  <done>Both leading-newline strings are split into a blank-line call followed by a message-only call; all tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Tasks 1 through 4 applied all 9 smell fixes across the source files. Changes have NOT been committed — they are staged or unstaged in the working tree, ready for your review:

    - Smell 1: Removed unused using directives from ParsedCommand.cs (System, System.Collections.Generic, System.Text) and CommandParserTests.cs (System, System.Collections.Generic, System.ComponentModel.Design, System.Text)
    - Smell 2: CommandOptions properties made init-only via record conversion (superseded by Smell 6)
    - Smell 3: TryParsePlaceArgs changed from public to private in CommandParser.cs
    - Smell 4: Direct test of TryParsePlaceArgs and its TheoryData provider removed from CommandParserTests.cs
    - Smell 5: Report() guard added — throws InvalidOperationException when robot has not been placed
    - Smell 6: ParsedCommand and CommandOptions converted to positional records; CommandParser updated to use positional constructor new CommandOptions(x, y, facing)
    - Smell 7: Table._width and _height declared as readonly
    - Smell 8: IsRobotPlaced() replaced by IsPlaced property on Simulator; Application.cs call site updated
    - Smell 9: Two leading-newline strings in Application.cs split into separate blank-line and message calls
  </what-built>
  <how-to-verify>
    1. Run the test suite: dotnet run --project ToyRobot.Tests
       All tests should pass.
    2. Review the diff: git diff
       Confirm changes across all six files match the intent above.
    3. Check key structural changes:
       - ParsedCommand.cs: two positional record declarations, no using directives above the namespace
       - CommandParser.cs: TryParsePlaceArgs is private; new CommandOptions(x, y, facing) constructor call
       - Table.cs: readonly on both _width and _height
       - Simulator.cs: IsPlaced property present; Report() has an InvalidOperationException guard as its first statement
       - Application.cs: _simulator.IsPlaced (no parentheses); two split WriteLine pairs (no leading escaped newlines in any string argument)
       - CommandParserTests.cs: no TheoryData method or test method referencing TryParsePlaceArgs directly
    4. When satisfied, commit the changes with an appropriate message (e.g. "refactor(07-01): fix 9 code smells") and then type your resume signal below.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to Wave 2, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- [ ] dotnet run --project ToyRobot.Tests — all existing tests pass
- [ ] git diff shows changes across ParsedCommand.cs, CommandParser.cs, Table.cs, Simulator.cs, Application.cs, CommandParserTests.cs
- [ ] No git commit has been made — changes remain in the working tree for developer review
- [ ] Developer has reviewed the diff, committed with an appropriate message, and typed "approved"
</verification>

<success_criteria>
- All 9 code smells from the post-phase-6 review are resolved
- All existing tests pass with zero failures
- Changes reviewed and committed by the developer before Wave 2 begins
</success_criteria>

<output>
After the checkpoint is approved and the developer commits, create `.planning/phases/07-code-cleanup/07-01-SUMMARY.md`
</output>
